# NetworkManager автозапуск

>$ sudo systemctl enable NetworkManager.service

# Подключение к Wifi через NetworkManager

>$ sudo NetworkManager
>
>$ nmcli dev wifi connect OrrHome
>
>$ iwconfig wlan0 essid OrrHome